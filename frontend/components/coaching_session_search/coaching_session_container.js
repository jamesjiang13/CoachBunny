import { connect } from 'react-redux';
import NewSearchForm from './search_form';

const mapState = (state, ownProps) => {
  return (null);
};

const mapDispatch = (dispatch) => ({

});

export default connect(
  mapState, mapDispatch,
)(NewSearchForm);

// deletable
